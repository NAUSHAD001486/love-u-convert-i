# API Base URL Configuration
# 
# Dev on same machine (desktop):
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
#
# Testing from other device on LAN:
# Leave this variable blank/unset, and the helper will automatically
# use http://<current-host>:3000 based on the device accessing the frontend
#
# Example: If frontend is accessed via http://192.168.1.100:3001,
# the API will automatically use http://192.168.1.100:3000
#
# NEXT_PUBLIC_API_BASE_URL=
